.page.resumes
  .page-header
    .container
      %h4 My Resumes
  = react_component("JibJob.ResumeList", {resumes: JSON.parse(@resumes_data), createPage: new_user_resume_url(current_user)}, {prerender: true})