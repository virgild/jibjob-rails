.user-resumes-page.page.small-white-content-page
  .container
    .user-resumes-page__title
      %span.glyphicon.glyphicon-file
      My Resumes
  #resumes

- content_for :body_javascript do
  :javascript
    $(function(){
      React.render(React.createElement(JibJob.ResumeList, { resumes: #{@resumes_data}, createPage: "#{new_user_resume_url(current_user)}", showCreateButton: #{current_user.has_available_resume_slot?} }), document.getElementById("resumes"));
    });